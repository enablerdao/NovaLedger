/**
 * ヒーローセクションコンポーネント
 * 注: このコンポーネントはHeaderコンポーネント内で既に実装されているため、
 * 現在は空の実装になっています。将来的にヒーローセクションを分離する場合に使用します。
 */
export const renderHero = (parentElement: HTMLElement): void => {
  // ヒーローセクションはHeaderコンポーネント内で実装済み
};